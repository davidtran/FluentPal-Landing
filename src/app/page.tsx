import Image from 'next/image';

export default function Home() {
  return (
    <main className="flex min-h-screen flex-col items-center justify-center p-24">
      <h2 className={`mb-3 text-2xl font-semibold`}>FluentPal</h2>
    </main>
  );
}
